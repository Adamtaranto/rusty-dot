[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rusty-dot"
version = "0.2.0"
description = "Fast dot plot comparisons of DNA sequences using a canonical ntHash k-mer index"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12,<3.15"
keywords = ["bioinformatics", "dotplot", "sequence-comparison"]
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    # Binary wheels are published for each of these, plus 3.14t.
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Free Threading :: 3 - Stable",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = ["matplotlib>=3.5", "numpy>=1.21"]

[project.optional-dependencies]
test = [
        "pytest-codspeed",
        "pytest-cov",
        "pytest",
        "toml",
        ]
dev = [
    "isort",
    "maturin>=1.0",
    "mypy",
    "numpydoc-validation",
    "pre-commit",
    "pydocstyle",
    "ruff",
]

docs = [
    "zensical>=0.0.57",
    "mkdocstrings-python>=2.0",
    # Zensical has no notebook support; scripts/notebooks_to_md.py renders
    # docs/tutorials/*.ipynb to Markdown before the build.
    "nbconvert>=7.0",
]

[project.urls]
documentation = "https://adamtaranto.github.io/rusty-dot/"
homepage = "https://github.com/adamtaranto/rusty-dot"
repository = "https://github.com/adamtaranto/rusty-dot"

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "rusty_dot._rusty_dot"
# Bundle the HTML report assets (template/CSS/JS) into wheels and sdists.
include = [{ path = "python/rusty_dot/_html/**/*", format = ["sdist", "wheel"] }]

[tool.pytest.ini_options]
addopts = "-v --cov=rusty_dot --cov-branch --cov-report=xml --cov-report=term"
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
    "private_data: benchmarks against private local assemblies (opt-in via RUSTY_DOT_PRIVATE_DATA=1; data never committed)",
]

[tool.ruff]
target-version = "py310"
line-length = 88
fix = true
exclude = ["docs"]

[tool.ruff.lint]
select = [
    "C", # mccabe rules
    "F", # pyflakes rules
    "E", # pycodestyle error rules
    "W", # pycodestyle warning rules
    "B", # flake8-bugbear rules
    "I", # isort rules
    "D", # pydocstyle rules
]
ignore = [
    "C901", # max-complexity-10
    "E501", # line-too-long
    #"I001", # isort-imports
    "B905", # `zip()` without an explicit `strict=` parameter
]

# Don't auto-fix docstring issues - they're too fragile
unfixable = ["D"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"] # Ignore all pydocstyle rules in tests
# The Shiny app is UI wiring: dozens of tiny reactive closures where
# per-function docstrings add noise.  app/core/ (the logic) keeps full D rules.
"app/app.py" = ["D103"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.format]
indent-style = "space"
quote-style = "single"

[tool.ruff.lint.isort]
force-sort-within-sections = true
known-first-party = ["rusty_dot"]
known-third-party = ["matplotlib", "numpy"]

[tool.isort]
profile = "black"
known_third_party = ["matplotlib", "numpy"]
known_first_party = ["rusty_dot"]
default_section = "THIRDPARTY"
force_sort_within_sections = true

[tool.numpydoc_validation]
checks = [
    "all",  # report on all checks
    "ES01", # but don't require an extended summary
    "EX01", # or examples
    "SA01", # or a see also section
    "SS06", # and don't require the summary to fit on one line
]
exclude = [ # don't report on checks for these
    '\.__init__$',
    '\.__repr__$',
    '\.__str__$',
]
override_SS05 = [ # allow docstrings to start with these words
    '^Process ',
    '^Assess ',
    '^Access ',
]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pydocstyle]
convention = "numpy"
match-dir = "[^\\.].*"     # matches all dirs that don't start with a dot
match = "(?!test_).*\\.py" # matches files that don't start with 'test_' but end with '.py'
